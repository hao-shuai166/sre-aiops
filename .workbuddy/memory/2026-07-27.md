# 2026-07-27 Work Log

## Session: Real K8s Client Implementation

- ludy 提出接入真实 K8s 集群，讨论后确定方案：三级 config auto-detection（显式路径 → in-cluster → ~/.kube/config）
- 容器化部署使用 load_incluster_config() + ServiceAccount RBAC，零额外配置
- 确认当前无可用 K8s 集群（Windows 本机无 ~/.kube/config，公有云机器未确认）
- 先实现代码，集群后续再配

### 代码变更

**修改 `adapters/k8s_client.py`：**
- `__init__` 新增 kubeconfig / context 参数
- `_load_config()`：三级 fallback（显式路径 → in-cluster → 默认 kubeconfig）
- `_get_pod_real()`：CoreV1Api.read_namespaced_pod → 映射为 mock 兼容格式
- `_get_events_real()`：CoreV1Api.list_namespaced_event + field_selector
- `_get_logs_real()`：CoreV1Api.read_namespaced_pod_log
- 统一 `_api_error()`：404/403/401 映射为结构化错误
- 异步调用通过 asyncio.to_thread() 包装同步 K8s client

**修改 `mcp/kubernetes_server.py`：**
- `KubernetesClient(mode=os.getenv("K8S_MODE", "mock"))`

**修改 `workflow/pod_crash_workflow.py`：**
- 同上，添加 `import os`
- `_k8s = KubernetesClient(mode=os.getenv("K8S_MODE", "mock"))`

**修改 `.env`：**
- 新增 `K8S_MODE=mock` 默认值

### 测试结果
- Mock 模式：5/5 场景通过（test_llm_rca.py）
- Real 模式：无集群时正确抛出 ConfigException

### 切换方式
```bash
# .env 中设置
K8S_MODE=real

# 或运行时
$env:K8S_MODE = "real"
```
