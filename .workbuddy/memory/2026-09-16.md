# 2026-09-16

- **两轮外部架构评审裁决完成**：用户拿来两份外部建议（AI 生成）。第一轮（三平面/Playbook/Hypothesis Manager/八组件 Runtime）裁决取 3 弃 5——采纳 Evaluation 层、异步调查 API、Capability 契约先行；否决 Playbook 硬引擎（是已退役的 v1 固定流程还魂）、State 大拆（其实已拆）。第二轮（按真实代码评审，质量高）确认 4 个真问题：evidence 不约束结论、全局单例并发缺陷、ReasoningControl 漂移、_CLUSTER 硬编码；用户与我共同裁决出最小实现包。
- **已敲定的最小实现包（优先级序）**：① Evidence 显式引用+校验（answer schema 加 evidence 字段，_valid_answer_decision 校验非空数组，agent_node 校验 ID 存在且至少含一条非错误证据，Diagnosis 挂 LLM 选择的而非全量）→ ② 请求级 InvestigationRuntime（contextvars.ContextVar 注入，图保持单例；解决 9/11 修复遗留的全局单例并发缺陷）→ ③ _parse_pod_name/_parse_namespace 抽到 agent/target_resolver.py + 轻量 TargetContext，删 _CLUSTER="prod" → ④ tail 参数改 integer schema(min1/max200) + get_container_logs 内部查询写入 metadata。不做：八组件 Runtime、facts/signals 语义层、ReasoningControl 变 LLM 自评终止器（保留步数熔断+强收尾）、get_container_logs 强制显式 container（自动解析保留，补审计元数据）。
- **关键实现接缝（已向用户讲明）**：runtime 与图单例生命周期冲突→contextvars；错误证据引用语义→"引用集中至少一条非错误证据"而非全禁（NotAvailable 用于排除假设是合法引用）。单测与校验实现同提交。
