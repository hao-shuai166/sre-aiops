# 2026-07-24 Work Log

## Session 1: Bootstrap + Architecture Walkthrough

- 完成身份 Bootstrap: 用户=ludy, 我的名字=Roxy, 正式风格, 北京, K8s SRE
- 更新 SOUL.md / IDENTITY.md / USER.md, 删除 BOOTSTRAP.md
- 用 Agent 工具完整读取所有源代码文件 (15 个 .py)
- 向 ludy 讲解了 6 层架构 + LangGraph 工作流拓扑 + LLM 注入点设计
- 生成两个可视化图表: 6-Layer Architecture, CrashLoopBackOff Workflow Topology, LLM Injection Points
- 生成项目状态报告: docs/PROJECT_STATUS.md

## Key Discussion Points

- ludy 问了 LLM 层如何工作: 解释 LLM 不是独立层, 而是注入到 Agent/Workflow 内部决策节点的推理引擎
- 推荐下一步做 LLM 接入, 从 rca_node 开始 (最有价值, 最容易验证)
- ludy 要求生成当前项目状态 md 文件

## Session 2: LLM RCA Integration

- ludy 确认下一步: "LLM 接入，从 RCA 节点开始"

### 新增文件
- `src/infrastructure_agent/llm/client.py` — LLMClient (OpenAI SDK 封装), generate() / generate_structured() 异步方法, 无 API Key 时返回 None 实现 graceful degradation
- `src/infrastructure_agent/llm/prompts.py` — RCA_SYSTEM_PROMPT (SRE 专家 persona) + build_rca_prompt() (将 Evidence + ReasoningSteps 注入 User Prompt)
- `src/infrastructure_agent/llm/__init__.py` — 更新导出: LLMClient, get_llm_client, RCA_SYSTEM_PROMPT, build_rca_prompt
- `tests/test_llm_rca.py` — 5 场景 E2E 测试

### 修改文件
- `src/infrastructure_agent/workflow/pod_crash_workflow.py`:
  - 导入 LLM 模块
  - rca_node 拆分: _fallback_rca() (V1 规则引擎) + async rca_node() (LLM 优先, 不可用时 fallback)
  - WorkflowState 字段重命名: _pod/_namespace/_cluster → wf_pod/wf_namespace/wf_cluster
- `src/infrastructure_agent/adapters/k8s_client.py`:
  - image_pull mock events: reason "Failed" → "ErrImagePull" + "ImagePullBackOff"
  - _pick_scenario: 新增 pod-name alias mapping (sched-fail → failed_scheduling 等)

### Bugs Fixed
- P0: WorkflowState 的 _pod/_namespace/_cluster 在 Pydantic v2 中是私有属性, LangGraph 节点间传递时丢失 → 所有场景退化为 app_error → 重命名为 wf_ 前缀
- P1: image_pull mock 缺 ImagePullBackOff event reason → classify_event 路由到 app_error
- P1: sched-fail pod 名不匹配任何 scenario key (key 是 "failed_scheduling") → _pick_scenario 新增 alias 映射

### Test Results
- 5/5 场景 fallback 路径通过 (无 API Key 时走规则引擎)
- LLM 路径代码就绪: 设置 OPENAI_API_KEY 环境变量后自动切换

## Session 3: DeepSeek Setup + Test Fix

- ludy 提供了 DeepSeek v4 pro API 凭据，要求配置环境变量
- 安装 python-dotenv，更新 client.py 自动加载项目根目录 .env 文件
- 创建 .env 文件: OPENAI_API_KEY / OPENAI_BASE_URL=https://api.deepseek.com / LLM_MODEL=deepseek-v4-pro
- 测试结果: LLM Available: True, 5/5 场景全部通过
- 修复 test_llm_rca.py 关键词匹配: OOMKilled 场景从纯英文关键词改为中英双语 (LLM 用中文回复时英文关键词无法匹配)
