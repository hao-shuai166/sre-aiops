# 2026-07-30

## Containerization — Docker + K8S deployment manifests

### Files created
- `Dockerfile` — multi-stage build (builder→runtime), non-root user, HEALTHCHECK, EXPOSE 8000
- `.dockerignore` — excludes .venv, .env, __pycache__, .workbuddy, tests, docs
- `k8s/deployment.yaml` — full stack: Namespace + Secret + ServiceAccount + ClusterRole + ClusterRoleBinding + Deployment + Service

### Files modified
- `pyproject.toml` — added `python-dotenv>=1.0.0` to dependencies (was missing, used by llm/client.py)
- `llm/client.py` — clarified comment: .env auto-load is dev-only, safely skipped in containers
- `tests/test_llm_rca.py` — updated keywords for ApplicationError and ConfigError (LLM Chinese output changed)

### Verification
- `pip wheel .` builds successfully (27KB wheel)
- 5/5 LLM RCA scenarios pass
- Docker not available on this machine — Dockerfile validated manually, build not tested
