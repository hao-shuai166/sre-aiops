# 2026-09-11

- **前端 v1 完成（Vue 3 + Vite + Element Plus）**：frontend/ 目录新建（package.json/vite.config.js/index.html/src），核心组件 DiagnoseConsole.vue——输入区+5场景快捷标签+加载计时+骨架屏+结果仪表盘（问题/根因/建议分条/置信度进度条/rca_mode徽章）+推理时间线（decision/tool/final 三色区分）+证据链手风琴（JSON 展示）。dev 模式 /api 代理 127.0.0.1:8000。
- **FastAPI 集成**：main.py 挂载 frontend/dist（存在性守卫 + html=True SPA 回退），同源访问无需独立 web 服务；顺手把 version 0.1.0→0.2.0（此前记录的杂项之一）。
- **构建与验证**：npmmirror 镜像装依赖（6分56秒），vite build 成功（1MB JS/363KB CSS，gzip 332KB——Element Plus 全量引入正常体量）。全链路验证通过：首页 200/资源可达/API 通路/错误态数据。
- **发现**：.env 的 OPENAI_API_KEY 在 9月4日仓库恢复时被重置为空——LLM 完整链路验证因缺 key 跳过；错误态展示反而得到真实验证（rca_mode=error 结构化返回）。**待用户回填 key 后跑一次完整诊断**。
- 启动命令：`.venv/Scripts/python.exe -m uvicorn infrastructure_agent.main:app --port 8000`，浏览器开 http://127.0.0.1:8000

- **Dockerfile 升级为三阶段构建**（用户新请求）：frontend-builder(node:22-slim + npmmirror) → backend-builder(python:3.12-slim + 阿里云 PyPI，恢复了 9/3 事故丢失的镜像源修复) → runtime(离线装 wheel + COPY 前端 dist + FRONTEND_DIST 环境变量)。main.py 加 FRONTEND_DIST 覆盖支持（容器内包装在 site-packages，源码相对路径失效）。.dockerignore 加 frontend/node_modules 和 frontend/dist（防止本地垃圾进镜像）。本地冒烟验证 FRONTEND_DIST 挂载通过。构建命令：docker build -t sre-agent:0.2.0 .（在含 frontend/ 的项目根执行）

- **Kind 集群远程访问方案讲解**（用户场景：Kind 在虚拟机里，要在宿主机浏览器访问 sre-agent 页面）。给出四方案：extraPortMappings（建新集群，最接近生产）> kubectl port-forward + SSH 隧道（现有集群最快，零改动）> NodePort > LoadBalancer+port80。关键命令：kind 配置映射 8000、kubectl port-forward svc/sre-agent 8000:8000、SSH -L。

- **修复前端 405 bug**：DiagnoseConsole.vue 的 fetch('/api/diagnose') 在生产构建下无 Vite 代理剥前缀 → POST 打到静态挂载 405。改为 fetch('/diagnose')（dev 与生产同路径），vite.config.js 代理规则同步加 /diagnose 和 /health 直转。已重新构建并验证产物。**用户虚拟机 Kind 里的旧镜像需重打**：docker build → kind load → rollout restart。

- **DeepSeek key 验证 + 代理坑定位**：用户提供新 key（sk-5564...9963），写入 .env（.env 同时重新移出 git 索引）。curl 直连 API 成功但 Python openai SDK 报 APIConnectionError——根因：本机代理（127.0.0.1:63222，Clash 类）拦截 Python 出站 HTTPS。清空 http_proxy/https_proxy 环境变量后 SDK 秒通。**完整诊断验证通过**：OOM 场景 rca_mode=llm、confidence 0.98、2 步收敛（status→metrics，正确跳过 events/logs）、根因引用 ev001/ev002 证据。注意：用户本机跑后端需先清代理变量或给代理加 api.deepseek.com 放行规则；虚拟机无此问题。
