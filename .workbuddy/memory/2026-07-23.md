## 2026-07-23 — agent-flow.md V2 评审

- 用户将 `docs/architecture/agent-flow.md` 从 V1 重写为 V2（v2.0），从 ~85 行扩展到 ~1078 行，新增 17 个章节
- 上一轮提出的 5 个历史问题全部解决：
  1. Investigation 路径 → 新增 §5 Investigation Workflow（结构化收敛：基础信息收集 → 发现异常方向 → 进入具体 Workflow）
  2. 反馈循环 → 新增 §10 Reasoning Loop（获取Evidence → 分析 → 信息是否足够 → 否则继续调用Tool）
  3. 证据状态载体 → 新增 §8 Agent State（含 user_question/current_workflow/problem_type/evidence/reasoning_history/diagnosis）
  4. Workflow Router → 新增 §7（LLM分类 + 规则判断，两层路由）
  5. 异常处理 → 新增 §13（标准化 JSON success/error 格式 + Agent 决策：重试/换源/降级/告知）
- 新发现 6 项待改进：
  1. Agent State 在架构图(§3)中被画成线性步骤，实际应为共享上下文（所有层读写）
  2. Reasoning Loop 缺终止条件（需定义最大迭代数 + LLM 自评）
  3. Evidence 结构过于简单（flat list，需结构化：timestamp/source/confidence）
  4. LangGraph 节点图(§14) 是线性链，未体现循环边（与 §10 矛盾）
  5. Tool 签名(§12) 缺参数定义（get_pod(namespace, pod_name)?）
  6. MCP Server(§11) 拆分策略未明确（K8s/Prometheus/Loki 是独立 Server 还是统一 Server）
- 亮点：Investigation 设计为结构化收敛而非 LLM 自由探索；两层路由务实；Evidence Driven 有正反例；演进路线清晰

## 2026-07-23 — agent-state-design.md 评审 + agent-flow.md 紧急修复

- 用户新写了 `docs/architecture/agent-state-design.md`（v1.0, 17 章节），对上次 6 项评审做出直接回应
- 已修复 3 项：
  1. Agent State 定位 → §2 声明「不是执行步骤」，画成共享上下文
  2. Evidence 结构化 → §8-9 补全 7 维字段 (id/type/source/timestamp/resource/content/confidence)
  3. Reasoning Loop 终止条件 → §13 reasoning_control (max_iteration=5 + confidence threshold + evidence satisfied)
- 发现新问题：
  - execution.iteration(§7) 与 reasoning_control.iteration(§13) 功能重叠
  - reasoning_control 未出现在 §4 顶层结构
  - 缺少 State 生命周期说明（创建→填充→循环→终止）
  - 建议添加「required evidence checklist」概念
- 执行了跨文档一致性修复：
  1. agent-flow.md §3 架构图：State 从线性步骤改为底部共享上下文（所有组件箭头指向它读写）
  2. agent-flow.md §8.2：旧 State 定义替换为指向 agent-state-design.md 的引用 + 六模块表格 + 简化 YAML
- 两份文档 State 字段名现已统一为 request/intent/execution/evidence/reasoning/diagnosis

## 2026-07-23 — tool-design.md 评审 + P0/P1 修复

- 用户新写了 `docs/architecture/tool-design.md`（v1.0, 18 章节），覆盖 Tool 接口定义、错误模型、权限分级、Evidence 转换全链路
- 评审发现 7 项问题（3 P0 + 4 P1），全部修复：
  - **P0-1**: 7 个 Tool 的 Evidence 从 2-3 维补齐为 state-design §8 的 7 维结构 (id/type/source(system+api)/timestamp/resource/content/confidence)
  - **P0-2**: §11 错误模型 `status` → `result`，消除与 §7 Output 中 Pod 业务 `status` 的命名冲突
  - **P0-3**: §7.3 K8s `get_logs`（容器级实时日志）与 §9 Loki `query_logs`（集群级中心化日志搜索）边界明确，双向交叉引用
  - **P1-4**: §3 架构图 + §14 流程图中 Evidence Builder 标注为「Tool Layer 统一组件，各 MCP Server 共享」
  - **P1-5**: query_trace 的 `time` 和 Loki 的 `range` 统一为 `time_range`
  - **P1-6**: get_events、get_logs、query_metric 补上 `cluster` 参数
  - **P1-7**: 新增 §5.2「Tool 发现机制」，说明 MCP `list_tools()` 动态发现流程、LangGraph ToolNode 绑定方式

## 2026-07-23 — pod-crashloopbackoff.md 评审 + 全量修复

- 用户新写了 `docs/workflow/pod-crashloopbackoff.md`（v1.0, 20 章节），第一个具体 Workflow 设计
- 评审发现 8 项问题（3 P0 + 3 P1 + 2 P2），全部修复：
  - **P0-1 Evidence 对齐**: §6.4 的 3 维 Evidence (type/source/confidence) 和 §7.3 的裸 source 字符串，全部改为 7 维结构 (id/type/source(system+api)/timestamp/resource/content/confidence)
  - **P0-2 Tool Input 补全**: §7 get_events、§9 get_logs、§10 query_metric 全部补全了 Input 参数（cluster/namespace/...），标注参数来源
  - **P0-3 Router 补全**: §8 新增 ImagePullBackOff (§8.3) 和 FailedScheduling (§8.4) 两条诊断路径，原 Configuration Error 移至 §8.5 并补充 Tool+指标
  - **P1-1 State 6模块映射**: §5 从 5 个模糊要点升级为完整的 6 模块对照表，每模块标注「本 Workflow 写入内容」
  - **P1-2 max_iterations → max_iteration**: §13.1 字段名对齐 state-design §13
  - **P1-3 Config Error 充实**: §8.5 补充 Tool 调用 (get_events/get_pod) + 关注点 (ConfigMap/Secret/PVC/SA)
  - **P2-1 LangGraph 细化**: §16 每个节点标注 LangGraph 类型 (function/tool/conditional_edge)，补全 5 路 Router 分支 + Decision Node 回边说明
  - **P2-2 State 转移快照**: §6.5 / §7.4 / §9.3 / §10.3 各 Step 追加执行前后 State 快照；§14.1 新增完整 State 转移示例（OOMKilled 路径）
