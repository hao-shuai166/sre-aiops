# 2026-09-07

- **架构讲解会话，细读代码新发现两个真实缺陷（未修，待排期）**：
  1. memoize 缓存跨请求污染：tools/k8s_tools.py L186 tool_registry 模块级单例，_cache 无 TTL 且 execute_tool_node 从不 clear_cache → 同一 Pod 第二次诊断命中第一次的旧缓存，状态已变场景（CrashLoopBackOff）会基于陈旧数据诊断。修法：每次调查开始清缓存或加 TTL。
  2. Evidence 计数器跨请求续号：agent_workflow.py L58 _builder 模块级单例，_counter 不重置 → 第二次请求证据 ID 从 ev005 起而非 ev001。功能无害（引用的是本次实际 ID），展示不规整。
- 已知未修杂项：main.py version 0.1.0 与 pyproject 0.2.0 不一致；trace step2/4 conclusion 复用 thought 冗余；classify_intent 仍是关键词规则。
- 架构讲解要点已向用户完整输出：LangGraph 选型逻辑（vs 手写/LangChain/OpenAI FC）、无状态 LLM 决策函数设计、三重输出校验、MAX_STEPS=8 强收尾、工具描述即提示词工程、O(n²) prompt 增长与无 checkpointer 是两大演进瓶颈。

- **项目结构讲解**：AST 实测包依赖方向——domain(零依赖) ← adapters ← tools ← llm ← workflow/agent，mcp 独立挂 adapters（P1 后成休眠路径）。向用户完整讲解了目录树、依赖分层、k8s vs manifests 双 yaml 目录的用途区分、__init__.py 门面模式。
